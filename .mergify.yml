# T4 triage re-verification (2026-09-23). Shape taken from
# https://docs.mergify.com/commands/restrictions ("Examples" - first block),
# transposed to `copy` and to an attribute the maintainer believes only they use.
pull_request_rules: []

commands_restrictions:
  copy:
    conditions:
      - title ~= ^\[copy-ok\]
